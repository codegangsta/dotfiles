# First window: Code
new-session -s blog -c ~/code/codegangsta.github.com -n code
  # First pane: vim
  send-keys "vim" Enter

  # Second pane:
  split-window -v -l 30%

  new-window -n hugo
    send-keys "hugo server -D" Enter

select-window -t code
select-pane -t 0
