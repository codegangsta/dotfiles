# First window: Code
new-session -s lsp -c ~/code/ck3_language_server -n code
  # First pane: vim
  send-keys "vim" Enter

  # Second pane:
  split-window -v -l 30%

select-window -t code
select-pane -t 0
