# First window: Code
new-session -s wiki -c ~/vimwiki -n wiki
  # First pane: vim
  send-keys "vim index.md" Enter

  # Second pane:
  split-window -v -l 30%

  new-window -n sync
    send-keys "git-sync-on-inotify" Enter

select-window -t wiki
select-pane -t 0
